## What was done

- Initialized the project structure: Next.js frontend and FastAPI backend.
- Implemented JWT-based authentication on the backend.
- Integrated NextAuth.js on the frontend for both email/credentials login and Google OAuth.
- Used Claude to help debug token validation logic between the frontend and backend.

## Problems encountered

- Hit a blocker when setting up the Anthropic API — the console required 2-step verification which I hadn't configured, which delayed getting API access.
- Connecting JWT (backend) and NextAuth.js (frontend) together was tricky at first because they handle tokens differently.

## Solutions

- Set up 2FA on my Anthropic account to get past the verification step.
- Broke the auth implementation into smaller pieces: first got JWT working standalone on the backend, then layered NextAuth.js and Google OAuth on top separately.

## Next plan

- Start working on file upload endpoint and document processing pipeline.
- Research chunking strategies for PDFs before implementing vector embeddings.

## Time spent

- ~4 hours